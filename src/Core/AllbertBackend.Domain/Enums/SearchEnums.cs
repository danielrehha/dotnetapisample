namespace AllbertBackend.Domain.Enums
{
    public enum SearchDateType {
        NotRelevant,
        Date,
        DateWithTimeRange,
    }

    public enum SearchLocationType
    {
        Static,
        DeviceLocation
    }
}
